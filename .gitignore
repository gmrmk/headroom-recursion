# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Node
node_modules/
.next/
.turbo/
# Build artifacts
dist/
build/
*.egg
# IDE
.vscode/*.local
.idea/
# OS
.DS_Store
Thumbs.db
# Lockfiles tracked, env not
.env
.env.local
# uv/pnpm reproducible state IS tracked
!uv.lock
!pnpm-lock.yaml

# Runtime artifacts
.dev-logs/

# Margaret COO PDF mirrors -- canonical artifacts live in
# ../osint-dashboard-research/phase6/; this directory exists for
# VSCode internal link convenience and should not be committed.
docs/margaret/

# Claude Code project-local state (agent memory + workspace settings)
.claude/

# graphify tool outputs -- regeneratable from current tree
graphify-out/
.graphify_ast.json
.graphify_detect.json
.graphify_uncached.txt

# Playwright e2e run artifacts (screenshots, traces, videos)
apps/web/test-results/

# TS incremental-build caches -- not source state
*.tsbuildinfo

# Probe HTML captures + body-fixture dirs under tools/dev -- throwaway
# empirical state behind the zendriver/DataDome routing decisions;
# the durable record is in tasks/lessons.md + the ADRs.
tools/dev/*.html
tools/dev/bypass-probe-bodies/
tools/dev/image-pipeline-bodies/
tools/dev/listing-failure-bodies/
tools/dev/tier-pressure-bodies/
