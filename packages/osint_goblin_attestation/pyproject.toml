        [project]
        name = "osint-goblin-attestation"
        version = "2026.05.0"
        description = "OSINT Goblin — osint_goblin_attestation package."
        requires-python = ">=3.11,<3.14"
        dependencies = [
  "osint-goblin-schemas",
  "osint-goblin-db",
  "osint-goblin-forensics",
  "pydantic>=2.9"
]

        [build-system]
        requires = ["hatchling"]
        build-backend = "hatchling.build"

        [tool.hatch.build.targets.wheel]
        packages = ["src/osint_goblin_attestation"]

[tool.uv.sources]
osint-goblin-schemas = { workspace = true }
osint-goblin-db = { workspace = true }
osint-goblin-forensics = { workspace = true }
