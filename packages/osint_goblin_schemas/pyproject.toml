        [project]
        name = "osint-goblin-schemas"
        version = "2026.05.0"
        description = "OSINT Goblin — osint_goblin_schemas package."
        requires-python = ">=3.11,<3.14"
        dependencies = [
  "pydantic>=2.9"
]

        [build-system]
        requires = ["hatchling"]
        build-backend = "hatchling.build"

        [tool.hatch.build.targets.wheel]
        packages = ["src/osint_goblin_schemas"]
